#include <stdio.h>

int main() {
    int N;
    scanf("%d",&N);
    if(num1>0)
       printf("Positive");
    else if(num==0)
       printf("Negative");
    else
       printf("Zero");
    return 0;
}